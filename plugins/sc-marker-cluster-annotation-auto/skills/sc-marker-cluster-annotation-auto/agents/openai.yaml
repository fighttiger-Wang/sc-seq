interface:
  display_name: "04 · 单细胞亚群注释（精细分型） v0.7.4"
  short_description: "完成同级亚群注释、全cluster UMAP身份复核与正式五表交付"
  default_prompt: "Use $sc-marker-cluster-annotation-auto to annotate known-parent subclusters, generate independent image-bound UMAP facts, apply auditable all-cluster retain-or-reassign decisions, and deliver the formally validated five-sheet workbook."

policy:
  allow_implicit_invocation: true
